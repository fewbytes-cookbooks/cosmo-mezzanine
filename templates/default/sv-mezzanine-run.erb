#!/bin/bash -e

# TODO: run as non-root

cd <%= @options[:virtualenv_dir] %>/app
<%= @options[:virtualenv_dir] %>/bin/gunicorn -b 127.0.0.1:<%= @options[:port] %> <%= @options[:app_name] %>.wsgi:application
